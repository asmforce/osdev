> страничная адресация
> swap-раздел (т.е. весь виртуальный(virtualbox) винт)
> путь в файловой системе (текстовый идентификатор);
> системный путь (index - файлы, каталоги, каналы fifo, устройства, etc);
